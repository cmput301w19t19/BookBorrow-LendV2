<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.y.bookborrow_lendv2">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignOutActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".EditBookDetail" />
        <activity
            android:name=".emptyActivity"
            android:label="@string/title_activity_empty"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".check_to_scan" />
        <activity android:name=".PrivateBookDetails" />
        <activity android:name=".OwnerHomeActivity" />
        <activity android:name=".RequestToOwner" />
        <activity

            android:name=".Search"

            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BorrowerMenu" />
        <activity android:name=".MyBookList" />
        <activity android:name=".profile" />
        <activity android:name=".BorrowerRequest" />

        <activity android:name=".RateToOwner" />
        <activity android:name=".home_page" />
        <activity android:name=".loginAct" />
        <activity android:name=".SearchResultForBook" />
        <activity android:name=".PublicBookDetails" />
        <activity android:name=".RateToBorrower" />
        <activity android:name=".BorrowBookList" />
        <activity
            android:name=".SearchResultForPeople"
            android:label="@string/title_activity_search"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>